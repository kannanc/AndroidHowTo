<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">
    <!-- 1. Button for standard alert dialog -->
    <Button android:id="@+id/alert_simple"
            android:text="Simple Alert"
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"/>
    <!--2. Button for AlertDialog with a single selection list-->
    <Button android:id="@+id/alert_list"
            android:text="Alert list"
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"/>
    <!--Alert Dialog with a multi-selection list. -->
    <Button android:id="@+id/alert_multi_list"
            android:text="Alert Multi list"
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"/>
</LinearLayout>